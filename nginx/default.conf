server {
    listen 80;
    server_name tenant1.local;

    location / {
        proxy_pass http://tenant1:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

server {
    listen 80;
    server_name tenant2.local;

    location / {
        proxy_pass http://tenant2:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

server {
    listen 80;
    server_name tenant3.local;

    location / {
        proxy_pass http://tenant3:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}